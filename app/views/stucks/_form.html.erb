

		<%= simple_form_for @stuck, :html => { :class => 'form-horizontal' } do |f| %>
		 <div class="row">
		    <div class="small-12 columns">
		      <%= f.input :content, :label => false, :placeholder => "Add Stuck" %>
		    </div>
			<div class="small-6 columns">
			  <%= f.button :submit, :class => 'button tiny radius round right' %>
			</div>
		  </div>
		<% end %>
